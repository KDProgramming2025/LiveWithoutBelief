config:
  validation: false

# Temporary configuration used solely for auto-correcting indentation.
# We disable buildUponDefaultConfig for the custom task so ONLY this rule runs.
# After running the detektIndentationFix task, this file and task definition
# should be removed in the same commit or a follow-up once indentation issues are resolved.
ktlint:
  Indentation:
    active: true
